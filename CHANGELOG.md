## Unreleased

* `AdaptiveNavigationScaffold` ganha `titleWidget` (`Widget?`) - título
  rico, vence sobre `title` (`String?`) quando os dois são passados.

## 0.0.1

* TODO: Describe initial release.
